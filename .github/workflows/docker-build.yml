name: Build Docker (manual)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Build Docker image (no push)
      run: |
        docker build -t daily-motivation-quotes-app:local .